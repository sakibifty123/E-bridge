<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ebridge_aust_03">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Teacher_login"></activity>
        <activity android:name=".Student_login" />
        <activity android:name=".Student_registation" />
        <activity android:name=".add_question_without_image" />
        <activity android:name=".Add_question_paper" />
        <activity android:name=".Add_faculty_page" />
        <activity android:name=".View_Admin_Added_Faculty" />
        <activity android:name=".Add_faculty" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>